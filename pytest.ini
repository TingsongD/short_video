[pytest]
# Scope default collection to the project suite — vendored trees carry
# their own dependencies (e.g. vendor/ tests need `requests`) and must
# never break a bare `pytest` run. `make test` already uses `pytest tests`.
testpaths = tests
norecursedirs = vendor node_modules .venv .git dist build docs apps contracts schemas
