# Copy settings into the project's private .env and save the file.
# Nonempty process environment > .env > config/secrets.toml.
# Empty placeholders preserve existing configured credentials.
ELEVENLABS_API_KEY=
VIRAL_OUTLIERS_API_KEY=

# Use the variable belonging to your Google key; these do not select a provider.
GEMINI_TTS_VERTEX_API_KEY=
# GEMINI_API_KEY=
# GOOGLE_API_KEY=
# GOOGLE_CLOUD_PROJECT=
# GOOGLE_CLOUD_LOCATION=global
# GOOGLE_GENAI_USE_VERTEXAI=true

# Google model access and the API route must be verified before generation.
