# F00 — Baseline and engineer onboarding

**Module / owner / source revision / completion date:** F00 · Devin (single builder) · accepted checkpoint `13a5d51` on `feat/factory` · 2026-09-16

**Scope implemented and intentionally deferred:** reviewed repository checkpoint, classified working-tree inventory, runtime-data ignore/backup policy, dependency/version inventory, frozen-contract digests, fresh test baseline, G/F gate status snapshot, scope banners. Deferred: no module code; F01+ follow dependency order.

**Assigned implementation scope (handover §2.5):** offline implementation — code, local tests, fixtures, local rendering — authorized by the user's 2026-09-16 instruction "update the plan, implement it". No new paid generation, research, purchase or public posting is authorized. Standing Drive delivery authorization remains scoped to finished videos. Live-only manual cases stay pending.

## Repository checkpoint (handover §2.4)

| Fact | Value |
| --- | --- |
| Starting branch / HEAD | `feat/m6-jimeng-canvas` @ `e6eea86` |
| Tracked-diff SHA-256 (pre-checkpoint) | `577fc4d6a0bb6c66315020be35f8db203de47ca5110e6b5acdb66c994fe8ec9f` |
| Code/test checkpoint | `b38f9b9` — `modules/batch/{audio,fast_render,local,render,runner}.py`, `tests/test_batch.py` (intended variable-timeline work; 344 tests green) |
| Documentation checkpoint | `13a5d51` — PROGRESS.md, Drive/batch docs, Hypit reviews, Vertex pilot report, handover pack |
| Accepted factory revision | `13a5d51` branched to `feat/factory`; `.gitignore` runtime rules + scope banners land on top of it |
| Deliberately remaining diff | `LONGFORM_PLAN.md` deletion — inspected: a 524-line abandoned "long-form conversion" plan marked PLAN ONLY, superseded by the factory direction. Not restored, not committed; left uncommitted per §2.4 pending owner disposition |
| Untracked runtime state | `data/costs/ledger.json` (SHA-256 `697d47a4f452ab1dd9e8532e32976ef3ca9cadd2e48db8e8dddb46cbe23da35a`, 12,204 B) — durable financial state, now git-ignored; private backup at `data/factory-qa/backups/ledger-20260916-preF00.json` (hash identical) |

**Classification of pre-checkpoint changes:** batch code/tests = intended legacy correction (variable `timeline_frames`, shipped with Haul 12); PROGRESS/Drive/batch docs + Hypit/Vertex/handover docs = evidence/handover documentation; `ledger.json` = generated runtime state (ignored, backed up); `LONGFORM_PLAN.md` deletion = unresolved-but-inspected; no unrelated in-progress work found.

## Runtime-data ignore policy (verified)

`git check-ignore` evidence: `data/costs/ledger.json`, `data/factory/factory.sqlite3`, `data/factory-qa/ws` all ignored; `data/costs/.gitkeep` and `tests/factory_fixtures/…` remain trackable. No private runtime file is staged; ledger bytes preserved locally and in the private backup above.

## Toolchain inventory

| Tool | Version | Role |
| --- | --- | --- |
| Python (.venv) | 3.14.3 | test runner, services |
| FFmpeg / ffprobe | 8.0 | local render/probe (required) |
| Node / npm | 26.7.0 / 11.19.0 | Hypit runtime, dashboard build |
| Hypit (pinned) | 0.1.8 via `scripts/hypit.sh` + `vendor/hypit-runtime` | composition/render (required for R1+) |
| dreamina-canvas CLI | 1.0.1 (commit `83aeb67`, cn) | Jimeng Canvas route (live-gated) |
| pytest deps | pytest, jsonschema, python-dotenv 1.2.3 | offline suite |

Credential presence only (never values): `.env` exists (mode 600, gitignored); `config/secrets.toml` supported; precedence confirmed = nonempty env > `.env` > TOML (`modules/common/config.py`). Live service availability not probed at F00.

## Frozen-contract digests

29 files hashed under `schemas/` + `tests/fixtures/`; full manifest in QA evidence (`data/factory-qa/`). Spot list: `shot_list.schema.json ee2e9c39…`, `niche_report.schema.json be2752d3…`, `tests/fixtures/media/good_video.mp3/…` — all recorded; any drift will fail the F00 regression check added in F01's suite (`tests/test_factory_qa.py` re-hashes on demand).

## Baseline test result

`make test` → **344 passed in 27.25s**, offline, zero skips/failures (2026-09-16). Matches the historical count; establishes today's reproducible base.

## G/F acceptance ownership (handover §2.3)

| System | Scope | Starting status |
| --- | --- | --- |
| `docs/gates.md` G0–G12 | legacy M0–M12 pipeline | G0 signed; G1–G12 unit criteria met, live gates pending — unchanged |
| `docs/viral-video-factory-module-tracker.md` F00–F35 | factory engineering/manual/live | F00 this report; F01–F35 planned |

Scope banners added to `AGENTS.md` and `GOAL.md` marking the swarm map / M1-restart instructions as superseded for factory work while preserving frozen-contract, offline-test, secrets and completion rules.

## Manual cases (offline; agent-executed, human sign-off pending)

| Case | Expected | Actual | Verdict | Reviewer |
| --- | --- | --- | --- | --- |
| F00-M01 | Reproducible baseline, exact install steps | `.venv` + `uv pip install pytest jsonschema python-dotenv==1.2.3`; `make test` 344/344; ffmpeg/node/hypit versions recorded | pass (agent) | devin-agent |
| F00-M02 | Offline dev available; live services shown unavailable without login/generation | Suite runs with no network; `.env` presence noted without loading credentials; no live calls attempted | pass (agent) | devin-agent |
| F00-M03 | Every change explained; frozen files/ledger unchanged; runtime data not staged | Diff manifest above; 29 frozen hashes match HEAD; ledger hash identical before/after; `git status` shows only intended paths | pass (agent) | devin-agent |
| F00-M04 | Restore preserves spending history; scope links readable | Backup hash verified equal; G/F table above; restore drill re-verified in F30 | pass (agent), restore drill pending F30 | devin-agent |

**Live qualification:** not applicable — F00 has no live cases. No paid calls made.

## Timing and external-effect counts

Zero external/paid effects. ~45 minutes of inspection/checkpoint work.

## Rollout / rollback

Factory features disabled by absence — nothing user-facing exists yet. Rollback = delete `feat/factory` branch; checkpoints `b38f9b9`/`13a5d51` on `feat/m6-jimeng-canvas` are independent of it. Runtime state (ledger) untouched by any rollback.

## Known limitations / follow-ups

- `LONGFORM_PLAN.md` deletion remains an uncommitted deliberate diff — owner to confirm disposition.
- macOS-only evidence; Windows support unclaimed.
- F00-M01–M04 verdicts are agent-recorded; a human reviewer's sign-off upgrades them from agent-verified to fully reviewed.
- R0 estimate: F01–F08 foundations — high uncertainty pre-F01; revisable after first export milestone R1.
