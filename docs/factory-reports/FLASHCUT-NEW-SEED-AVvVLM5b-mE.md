# New seed: recovery patched; live acceptance paused

> Historical checkpoint report. The run subsequently completed A/B/C/D, QC,
> Compare and verified delivery. See [current status](FLASHCUT-CURRENT-STATUS.md)
> and [completion evidence](FLASHCUT-CONTEXT-REUSE.md). Paused-state descriptions
> below are retained as history, not current operating instructions.

Updated 2026-09-22 UTC. The user superseded the earlier acceptance setup:
clear past jobs, start a brand-new run from
https://www.youtube.com/shorts/AVvVLM5b-mE, and approve video work under $50 by
default. The communicated conservative interpretation is **$50 total per run,
including A/B/C/D and repairs**, not $50 per clip. Publishing remains disabled.

**Latest checkpoint (05:42 UTC):** compact request-format recovery was deployed
and Google accepted both corrected requests. Whole-video analysis passed; the
window answer's coverage-gap prose is preserved in a provisional local recovery
bundle. Its required shared clarification returned **HTTP 500 INTERNAL** and
has an unknown outcome. The run is paused with no new finals. Held/ambiguous
estimates total **$2.021048**, not confirmed charges, within the same $50 cap.
See [current recovery evidence](FLASHCUT-FORMAT-RECOVERY-QUOTE.md) for the
bounded quote, regressions and exact request identity. Earlier checkpoints
below are retained as history, not the current error.

## Completed

- Downloaded the exact new seed using the installed Hypit downloader, without
  paid generation. Saved at
  `/private/tmp/factory-flashcut-acceptance.sAinMF/new-seed/source.mp4`.
- Probed video: 720×1280, 30 fps, 423 frames, 14.100 seconds. Audio is present,
  14.141 seconds. The local helper completed cleanly: **423 decoded and encoded
  frames**, plus **678,456 audio samples**. Audio measurements are available;
  rhythm was classified unreliable rather than inventing a beat grid.
- The prior explicitly approved four shared ceilings were raised by $3.26 each
  before the seed changed. No provider request or new autorun was submitted.
- Consistent snapshots and final fingerprints are retained under
  `/private/tmp/factory-flashcut-acceptance.sAinMF/before` and `approved-limits`.
  All 16 existing final files matched at snapshot time.
- The interrupted offline test for a source-analysis-only stop was stopped;
  its newly created test file was removed because that work was superseded.
  No product implementation of that stop was made or deployed.

## New run and retained history

The existing archive service reports **zero eligible records**. Paused,
blocked, awaiting-review or unresolved work and related dependencies remain
protected. No jobs, files, holds or historical records have been deleted,
archived, retried or settled.

The subsequent instruction, “generate so i can see them in compare”, was
executed with history retained, as communicated to the user. No destructive
reset was performed. Do not weaken archive safeguards or discard unresolved
accounting.

Run `auto-2c9de6ddf2e34d5c` uses seed `seed-youtube-c8fb2c2ae943d391`, source
SHA-256 `026ff7cd811e5d78e80b88bd77d3fa346281ef883799641366b00f227d78bc5f`,
and profile `flashcut_hypit.v1`. A/B/C/D generation, final QC, Compare and
verified Drive delivery are intended; publishing remains disabled.

- Dedicated selected budget `avvvlm5b-me-20260922-usd` enforces **$50 cumulative**
  including repairs and unresolved commitments. Required shared ceilings were
  increased to their existing commitments plus that allowance; audit events
  preserve the authorization and old amounts. Historical holds are unchanged.
- A non-billable ElevenLabs subscription check found 41,118 existing credits
  available on Starter with credit extension disabled. The run is bounded to
  10,000 included credits through `avvvlm5b-me-20260922-tts`; no top-up,
  subscription change or overage is enabled. No speculative USD conversion.
- This run's cap is configured. A general persistent $50 default for every
  future dashboard run is **not yet implemented**.
- Gemini/Jev routes have narrow, expiring acceptance bindings to this exact
  run/source, not fabricated global live qualifications.
- The API and one worker were safely restarted while idle. The
  `new-run-before-staging` snapshot and verification showed all 16 protected
  final files and all existing records unchanged at staging.
- Three Jev shadow requests succeeded. The Gemini analysis envelope reserves
  at most $2.322601 for up to six understanding/clarification requests; this is
  part of the $50 cap, not extra authority.

## Recovery and next checks

At 04:30 UTC the run paused before Gemini submission because the saved Google
credential refreshed but exposed no verifiable account identity. No account
check was bypassed. The user completed Google sign-in; all Google routes then
verified `david.dai@robanka.com`. The run resumed from saved evidence without
repeating PE, audio or Jev work.

The resumed analysis hit two live integration failures:

1. Whole-source request `:0` and window request `:2` returned HTTP 200 with
   `MAX_TOKENS`. Their 8,192-token limit includes reasoning: the whole-source
   response used 7,863 thinking tokens and returned only 322 answer tokens.
   The incomplete results were correctly not accepted as valid analysis.
2. Window request `:1` returned `STOP` and JSON, but validation rejected an
   observation spanning 0–7 seconds supported by the two overlapping supplied
   windows [0,4] and [3,7]. The validator currently requires a single window
   to contain the entire observation. Repair must validate continuous union
   coverage, not permit uncovered gaps or invent timestamps.

Window request `:3` succeeded. All four requests are now terminal at the job
layer. The three unusable answers retain their original unknown attempts and
ambiguous reservations; none was replayed, refunded or marked successful.
Their saved provider responses permit evidence-backed recovery design, not an
assumption of no charge. The current run has $0.763913 in held/ambiguous
estimates (not confirmed billed charges), including Jev and these requests.

The user approved patching and continuing within the existing $50 cap.
The following recovery was implemented and exercised; the current pause is
new and is described below. No spending-cap increase was made in this patch.

Post-attempt historical comparison: all 16 old final bindings/file hashes,
3,135 old records, 645 old jobs, 408 old attempts and all old reservations
remain unchanged. Exactly five old budget rows differ from the pre-run
snapshot: the four explicitly raised USD ceilings and prepaid-credit ceiling.

Do not replay unknown submissions or resume historical jobs. Four new finals,
native Hypit rendering, authoritative final QC, Compare playback and Drive
delivery remain **unverified until completed**.

## Approved recovery outcome — 2026-09-22 05:10 UTC

### Fixed and verified

- Window validation accepts continuous union coverage of cited overlapping or
  adjacent windows, but still rejects gaps, uncited support and invalid times.
  The real saved initial window `:1` was recovered locally without another
  provider call or alteration of its original receipt/attempt/reservation.
- A frozen supplemental recovery plan allows at most two distinct replacement
  requests for proven returned `MAX_TOKENS` responses. Each is freshly quoted
  with 32,768 output tokens and MEDIUM thinking, rather than silently changing
  the old request identity. Both live replacements returned `STOP`: the token
  exhaustion was resolved. Unknown transport outcomes cannot use this path.
- Proven completed responses release execution capacity only, with an audit
  record. Original unknown accounting, receipts and failed job statuses remain
  unchanged. Four retained dispatch slots had exhausted the four-slot pool;
  these were released after matching their private HTTP response evidence.
- Structured correction uses the existing persisted two-round clarification
  ledger; restart does not reset it. Normal missing-evidence clarifications can
  group questions that are covered by the same actual media. Request schema
  bytes/tokens are included in quotation and bounded payload calculations.
- Status APIs report all consumed clarification rounds, including structural
  correction. HTTP 400 recovery now has a specific technical pause message.

### Current blockers — not accepted as successful analysis

1. The replacement overview returned invalid evidence IDs, a zero-duration
   observation, invalid beat confidence and misplaced creative context. Its
   complete raw response is retained, not manually rewritten into evidence.
2. The other replacement's caption observation spans 7.5–10.5 seconds across
   a real gap in cited windows (8.5333–8.9). Its late reaction begins at 11.5,
   while its cited video starts at 11.9. These are grounding failures, not the
   overlapping-window validator bug. No timing or evidence check was relaxed.
3. The first schema-constrained clarification was rejected with HTTP 400
   `INVALID_ARGUMENT` before acceptance. The saved executor diagnostic proves
   the rejection; the existing accounting path released that request's $0.094908
   estimate. No historical or ambiguous charge was settled. Google documents
   that schema complexity can cause this error, but the exact incompatible
   constraint in this request is **not yet isolated**:
   [Google structured-output guidance](https://docs.cloud.google.com/gemini-enterprise-agent-platform/models/capabilities/control-generated-output).

The run is paused at `video_analysis`, with **one of two clarification slots
claimed**, both supplemental replacements consumed, and no production draft,
new footage or finals. More transport attempts need a qualified request format
and an explicitly frozen revised bounded recovery quote; do not reset counters
or replay old requests. The original five missing coverage candidates from the
saved window response also remain subject to grounded clarification.

Run accounting at this checkpoint: **$1.298942 held/ambiguous estimates**
($1.143643 ambiguous + $0.155299 held), against the unchanged $50 cap. These are
not provider-confirmed invoiced charges. No ElevenLabs speech was generated.

### Verification and deployment

- Test-first regressions failed before the relevant implementation changes.
- Focused offline suite: **77 passed** (provider payload/recovery, windows,
  envelope, credentials, operations and repair accounting).
- Full recovery offline end-to-end: **1 passed in 195.81 s**. Fake remote
  responses exercised truncation → malformed completed replacement → bounded
  structured clarification → four real local native Hypit finals, Compare-ready
  bindings and simulated verified Drive delivery. Paid transports were mocked.
- Earlier bounded-recovery end-to-end: **1 passed in 209.44 s**; budget refusal
  check passed in 26.82 s. The complete backend/frontend suite was not rerun for
  this recovery patch; its previous release results remain separate evidence.
- HTTP 400 dashboard-recovery regression: **1 passed in 23.55 s**; proves a
  specific action message and exactly one rejected request, not a retry loop.
- Deployment snapshot: `/private/tmp/factory-flashcut-acceptance.sAinMF/before-structured-recovery`.
  Owned services were quiescent; only the two pending jobs of this new run were
  retained. API PID 62723 and worker PID 62735 were registered on restart.
- Post-recovery audit: all **16 historical final bindings/file hashes**, 3,135
  historical records, 645 jobs, 408 attempts, 505 reservations, 2,676 reservation
  lines, 236 artifacts and 182 effect bindings remain unchanged. Five budget
  differences from the pre-run baseline are the earlier explicit authorization,
  not changes made by this patch. No historical jobs were resumed.

The new route is still acceptance-scoped, **not globally live-qualified**.
