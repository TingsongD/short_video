# Current codebase, documentation and development-log review

Reviewed source: `52c6ba6` (initial working tree clean), 2026-09-19.
Scope: current factory, dashboard, legacy pipeline entry points, requirements,
acceptance tracker, prior reviews and DEV_LOG. This is a whole-checkout review,
not a claim that every possible defect has been enumerated. No application
implementation, production database, credentials, provider account or frozen
contract was changed. Added only this report and isolated review probes.

P1 = address before relying on unattended paid production or its acceptance
records; P2 = functional/recovery gap; P3 = lower-impact legacy hardening.

## Confirmed code and recovery issues

| ID | Priority | Remaining issue and consequence | Source |
| --- | --- | --- | --- |
| C01 | P1 | **Autorun can queue duplicate paid work after a crash.** `_run_effect` commits preparation, authorization and job enqueueing before persisting the job IDs on the run. `EffectWork.prepare` generates a random plan ID on every invocation. A process loss after queueing leaves authorized jobs behind; restarting the saved run creates a second independently authorized set. The documented claim that effect plans are content-keyed is false here. Persist the run-to-plan association atomically with queueing, or use a stable run/stage operation identity and reconcile it first. | [autorun/service.py:440](../../modules/factory/autorun/service.py:440), [effect_work.py:47](../../modules/factory/services/effect_work.py:47) |
| C02 | P1 | **Music can bill the remaining balance as the operation's charge.** The adapter falls back from `x-credits-charged` to `x-credits-remaining` and exports either as `actual_credits`. If only the balance header exists, the ledger settles the account balance as spend, possibly triggering a false overrun and blocking all further effects. A remaining balance is not a charge; retain an unresolved estimate when no charge evidence exists. | [music.py:75](../../modules/factory/providers/music.py:75), [effect_work.py:107](../../modules/factory/services/effect_work.py:107) |
| C03 | P1 | **An old final-review result can approve replacement bytes.** Final QC requests bind the original artifact/hash, but after they finish `_stage_final_qc` fetches the current final and stamps the old verdict onto its current hash/binding. It does not compare that final to the reviewed request. `_finals` also lacks the revision filtering used by `experiment_results`. A rerender/revision while QC is pending can receive a verdict for a different video. Bind returned evidence to the submitted artifact and invalidate/requeue only when that identity changes. | [autorun/service.py:1393](../../modules/factory/autorun/service.py:1393), [autorun/service.py:1427](../../modules/factory/autorun/service.py:1427) |
| C04 | P1 | **Failed media scans count as passing reviews.** `_ffmpeg_report` converts missing executables and timeouts to an empty string and ignores nonzero exit codes. `inspect_asset` interprets an empty report as no black/frozen frames and returns `pass`, including a note claiming the scan ran. This review releases generated assets to composition. Require successful decoding/scanning; failed inspection must remain failed or uncertain. | [autorun/review.py:125](../../modules/factory/autorun/review.py:125) |
| C05 | P2 | **Repeated narration loses per-segment fitting and attachment.** `_stage_tts` keys both synthesis and fitting only by normalized text and retains the first segment. `AudioWork.attach` requires that segment's ID and target interval. Repeating the same line in the opening and ending therefore produces only one fitted/bound segment; the other is not attached. Reuse synthesis by voice/text, but fit and bind every distinct segment/target. | [autorun/service.py:899](../../modules/factory/autorun/service.py:899), [audio_work.py:85](../../modules/factory/services/audio_work.py:85) |
| C06 | P2 | **Long autoruns cannot synthesize more than 20 unique lines.** Autorun passes all fresh TTS requests in one batch, while `EffectWork.prepare` rejects more than 20 operations. A 20-beat control with three distinct treatment lines already exceeds this limit. There is no chunking/resumable batch handling. Failures occur after earlier paid analysis, despite the factory's long-reference scope. | [autorun/service.py:924](../../modules/factory/autorun/service.py:924), [effect_work.py:31](../../modules/factory/services/effect_work.py:31) |
| C07 | P2 | **The instructed recovery from flagged final QC does not work.** A flagged run says to review the finals in Compare and Resume. Resume retains `qc_jobs`; the final-QC stage reads the same completed machine results, writes new copies of their verdicts, and pauses again. It neither consumes a subsequent bound human resolution nor invalidates the old QC result for a repaired final. Implement an explicit, audited resolution or rerender/review path. | [autorun/service.py:147](../../modules/factory/autorun/service.py:147), [autorun/service.py:1387](../../modules/factory/autorun/service.py:1387) |
| C08 | P2 | **Several pause actions cannot be performed on the existing run.** `limit_too_low` says to raise the limit and Resume, but resume accepts only budget changes and ignores `limits`. It also cannot renew `valid_until`, so a run that outlives its default 24-hour authority cannot receive refreshed authority through this interface. The UI's “per-operation limits” are actually checked against an entire effect plan's total, making the label inaccurate too. Support explicit, audited parameter/authorization revision and label the limit's scope accurately. | [autorun/service.py:147](../../modules/factory/autorun/service.py:147), [autorun/service.py:393](../../modules/factory/autorun/service.py:393), [AutoRunScreen.tsx:144](../../apps/factory-dashboard/src/features/autorun/AutoRunScreen.tsx:144) |
| C09 | P2 | **Requested options are silently dropped when a route is unavailable.** `visual_reviews=true` still reaches done if the analysis adapter/account is absent; requested generated music is similarly removed. Notes disclose the downgrade afterward, but the operator never selects it. Pause with the missing capability and allow an explicit change of scope instead of completing a weaker run. | [autorun/service.py:751](../../modules/factory/autorun/service.py:751), [autorun/service.py:1355](../../modules/factory/autorun/service.py:1355) |
| C10 | P1 | **Estimated settlements cannot later be corrected to actual charges.** Once settled, any different amount or settlement kind raises `settlement_identity_conflict`. Thus a `usage_estimate` cannot become an `invoice_confirmed` charge—even at the same amount with a stronger evidence type. DEV_LOG records 40 holds closed using estimates, so this is relevant to recorded operations. Preserve original entries but add auditable adjustment/evidence-upgrade entries; otherwise both headroom and overruns can remain wrong. | [budget/service.py:195](../../modules/factory/budget/service.py:195), [DEV_LOG.md:1178](../../DEV_LOG.md:1178) |
| C11 | P2 | **An overrun has no implemented reconciliation/unblock path.** Settlement writes `meta.spend_overrun`; preparation and submission refuse further dispatch while it exists. Source-wide search found checks and insertion, but no audited resolution/removal path. Raising the ceiling does not clear it, and the reconcile/restore interfaces do not resolve it. Keep the conservative block, but provide an explicit financial-review resolution instead of requiring direct SQL edits. | [budget/service.py:212](../../modules/factory/budget/service.py:212), [execution/executor.py:113](../../modules/factory/execution/executor.py:113), [operations/reconcile.py](../../modules/factory/operations/reconcile.py) |
| C12 | P1 | **Stack scripts are not scoped to this checkout.** Both scripts identify API/worker processes only by `modules.factory.cli serve/worker`. `factory-down.sh` can kill another checkout's workers; `factory-up.sh` can mistake those workers for this project's services and skip startup. The project-specific Hypit profile does not scope these process-name searches. Use verified PID/creation-time/workspace ownership records. | [factory-down.sh:8](../../scripts/factory-down.sh:8), [factory-up.sh:16](../../scripts/factory-up.sh:16) |
| C13 | P2 | **Live manual-post verification has no wired implementation.** Configured publishing constructs `UploadPostPublisher` without `verifier`; its `verify_post()` then always raises `platform_verifier_unavailable`. `register_manual()` calls it when verification is requested. Adding credentials and qualification evidence alone cannot make verified manual registration work. Implement the platform/account-bound verifier or expose this capability as unavailable. | [configured.py:114](../../modules/factory/providers/configured.py:114), [publisher.py:142](../../modules/factory/integrations/publisher.py:142), [publishing/service.py:409](../../modules/factory/publishing/service.py:409) |
| C14 | P2 | **Valid normalized TTS alignment can be rejected after synthesis.** The adapter prefers `normalized_alignment` but requires its characters to exactly equal the unnormalized request. For a request `2`, a response with raw alignment `2` and normalized alignment `two` is rejected even though the raw alignment matches. The synchronous receipt stays ambiguous after the paid response. Align validation with the selected text representation and preserve the successful audio/receipt. | [elevenlabs.py:58](../../modules/factory/providers/elevenlabs.py:58), [synchronous.py:41](../../modules/factory/providers/synchronous.py:41) |
| C15 | P2 | **Uncertain product observations are promoted without new visual evidence.** `auto_review_beats` replaces the provider's `uncertain`/`unresolved` confidence with `reviewed` when a description, sufficient duration and overlapping transcript exist. It does not resolve the visual uncertainty or inspect the relevant frames; even an explicitly obscured garment detail is upgraded. Those confidence labels feed the blueprint acceptance gate. Preserve unresolved confidence until a qualified review supplies evidence resolving it; transcript overlap alone is insufficient. | [autorun/review.py:12](../../modules/factory/autorun/review.py:12), [autorun/service.py:607](../../modules/factory/autorun/service.py:607), [analysis/review.py:37](../../modules/factory/analysis/review.py:37) |
| C16 | P2 | **The analysis timeline's coverage gate checks only three instants.** `save_timeline` tests the start, midpoint and near-end, so sections covering only 0–1s, 50–51s and 99–100s pass for a 100s reference. `completeness_missing` later checks only that the timeline is nonempty. The human-facing analysis can therefore be marked complete with 97% of its timeline omitted, even though downstream blueprint partition checks provide a separate safeguard. Validate the union of intervals over the full reference duration. | [analysis/deep.py:498](../../modules/factory/analysis/deep.py:498), [analysis/deep.py:83](../../modules/factory/analysis/deep.py:83) |

## Remaining specification and workflow gaps

| ID | Remaining work | Evidence / boundary |
| --- | --- | --- |
| W01 | **Autorun completion stops before the repository's video-completion workflow.** It sets `succeeded`/“Complete” after local rendering/QC and merely notes that delivery is pending. Verified Drive upload, per-video cleanup and port verification are separate manual actions. Preserve any genuine human acceptance checkpoint, but represent that state explicitly and continue delivery/cleanup under its existing authority after acceptance. The later operator-aborted delivery must remain an abort, not be retried by this review. | [autorun/service.py:1339](../../modules/factory/autorun/service.py:1339), [AGENTS.md:current rule](../../AGENTS.md), [DEV_LOG.md:1192](../../DEV_LOG.md:1192) |
| W02 | **The deterministic adaptation fallback does not produce original control copy.** Its documented behavior keeps A verbatim from the source transcript, and the speech-fit fallback can restore source copy. This differs from the handover's original-copy requirement. Source-derived text may be suitable for an explicitly approved close-copy workflow, but that exception is not represented by the current factory specification or a run-level choice. | [autorun/scripts.py:1](../../modules/factory/autorun/scripts.py:1), [handover:57](../viral-video-factory-handover.md:57) |
| W03 | **Billing evidence and the recorded overrun need reconciliation.** DEV_LOG records roughly 2.54M microdollars over an 18.37M cap and 40 estimates settled at reserved amounts. This review did not inspect the operational DB or establish the historical cause. Preserve the evidence and reconcile provider usage/invoices against every authorization/reservation before claiming cap enforcement was demonstrated. Vertex currently saves token usage but does not turn it into the settlement fields consumed by production/effect workers; many results remain unverified holds until operator settlement. | [DEV_LOG.md:1178](../../DEV_LOG.md:1178), [vertex.py:250](../../modules/factory/providers/vertex.py:250), [production/plan.py:274](../../modules/factory/production/plan.py:274) |
| W04 | **The full live release/learning journey remains unqualified.** The log describes a real four-render autorun, followed by aborted Drive delivery. That does not establish accepted/delivered four-variant production, exact native publication/account/cancellation behavior, or genuinely elapsed analytics and next-round decisions. Complete the applicable scoped qualification and human acceptance, with separate publication authorization. Do not repeat already-qualified analysis/music pilots merely because an older tracker says pending. | [DEV_LOG.md:1117](../../DEV_LOG.md:1117), [publishing-learning handover:526](../factory-publishing-learning-handover.md:526) |
| W05 | **F-module and legacy G-gate acceptance remains unsigned.** F00–F34 still say engineering/manual `in_progress`; F35 is blocked. G1–G12 are independently pending. Reconcile module checklists with later repair/live evidence, identify exact unmet criteria and obtain the required sign-offs. A passing regression suite does not sign these gates. | [factory tracker:60](../viral-video-factory-module-tracker.md:60), [gates.md:6](../gates.md:6) |
| W06 | **Status documentation is contradictory and stale.** PROGRESS ends at the September 16 Vertex pilot, while the September 19 log contains new production and financial incidents. The tracker says all live qualifications remain pending despite the log's scoped analysis/music/Vertex qualification. The previous review says its remaining qualification section is accurate, but that section still claims uncommitted work and disabled optional music. DEV_LOG says newest entries are on top while September 19 entries are appended at the bottom; its final shutdown note says the script stops only API/worker although the current script also stops Hypit programs/runtime. Update a single current status index, preserving dated historical evidence. | [PROGRESS.md:642](../../PROGRESS.md:642), [DEV_LOG.md:3](../../DEV_LOG.md:3), [DEV_LOG.md:1203](../../DEV_LOG.md:1203), [previous review:55](REVIEW-2026-09-18.md:55) |

Retired Viral Outliers research is a deliberate scope decision in DEV_LOG, not
an instruction to reactivate or purchase credits. Its remaining UI/API should
clearly communicate that the route is unavailable. Similarly, historical
provider balances and old credential-setup tasks are not current account facts.

## Lower-impact legacy items still present

- **L01 (P3):** readback labels are assigned by positional `zip`; reordered,
  shortened or extended configuration can silently mislabel/drop windows.
  [windows.py:13](../../modules/analytics/windows.py:13).
- **L02 (P3):** `write_task` derives a fallback directory from an unsanitized
  subject/video ID. Absolute paths or traversal can escape the production root
  when this helper is called without an explicit directory. The usual caller
  supplies a directory, so this is a dormant helper defect.
  [task_builder.py:48](../../modules/assemble/task_builder.py:48).
- **L03 (P3):** short scripts that cannot be split into four clauses yield fewer
  than four shots and fail later schema validation. Give a direct script-stage
  error or implement a valid splitting policy.
  [shots.py:49](../../modules/script/shots.py:49).
- **L04 (acceptance):** legacy hook curation, radar-derived format refresh,
  pinned voice selection (`config/system.toml` still has an empty voice ID),
  full MPT subtitle validation and live weekly-cron verification remain in the
  independent legacy backlog. Working factory voice/Hypit routes do not finish
  those legacy tasks. [REMAINING_ISSUES.md](../REMAINING_ISSUES.md),
  [system.toml:27](../../config/system.toml:27), [gates.md](../gates.md).

## Verification and limits

- Saved [12 offline probes](probes/review_2026_09_19.py) reproduce C01–C06,
  C08–C10 and C14–C16. **12 passed**: their assertions confirm the bad behavior,
  not correctness. No provider calls are made. Other findings are traced code
  paths or explicitly labeled documentation/acceptance gaps.
- Dashboard: **30 tests passed**; TypeScript/Vite production build passed.
- Backend full-suite rerun: **1,085 passed**, two dependency deprecation
  warnings, **847.44 seconds**, with local process-inspection permissions.
  The restricted run was interrupted after 224 passed and two process-
  supervision failures; both passed in the permitted full run and are not
  counted as code defects. The green suite does not cover the separately
  reproduced failures above.
- Review does not verify live provider protocols, current balances, invoices,
  private runtime records or human creative acceptance. Previously repaired
  R/N/Q findings are not automatically reopened by their old reports.

Priority order: eliminate duplicate paid work and incorrect accounting; enforce
artifact-bound fail-closed QC; repair autorun recovery/bindings; then complete
delivery, native verification, financial reconciliation and acceptance records.
