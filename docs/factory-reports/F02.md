# F02 — Factory contracts and immutable revisions

**Module / owner / source revision / completion date:** F02 · Devin · `feat/factory` post-F01 · 2026-09-16

**Scope implemented:** all §7.2 entity records (Seed, MetricObservation, ProductSnapshot, ReferenceBlueprint, FormatTemplate, ExperimentRevision, VariantPlan, GenerationRequest, PriceAssessment, Authorization, Job, Attempt, Artifact, AssetUse, Review, Delivery, Publication, MetricSnapshot, Decision) with typed `ContractError` validators; rational frame clock with end-exclusive `[start,end)` intervals and tiling checks; integer microdollar/credit `Money`; `accept()`/`revise()` immutable revision lifecycle; frozen `contracts/v1` schema-version registry; legacy→factory `CompatibilityReport`; factory→legacy `asset_manifest` conversion that validates against the frozen schema and refuses Vertex provenance or >7 shots rather than mislabeling.

**Public interfaces and files changed:**
- `modules/factory/domain/` — `errors.py`, `clocks.py`, `money.py`, `records.py`, `revisions.py`, `legacy.py`, `to_legacy.py`, `errors_compat.py`
- `modules/factory/contracts/v1/__init__.py` — 23 schema-version strings + `check_version`
- `modules/factory/qa/cases_f02.py` — 4 case implementations
- `tests/test_factory_contracts.py` — 35 tests

**Prerequisite versions and migration impact:** pure in-memory domain layer; no storage touched.

**Targeted automated commands and results:** `pytest tests/test_factory_contracts.py` → 35 passed.

**Full offline suite:** `pytest tests -q` → **404 passed** (369 + 35; one secrets-sweep fix: key-shaped literal in `test_factory_qa.py` split into concatenation so the sweep stays green).

**Manual cases (agent-executed; human sign-off pending):**

| Case | Expected | Actual | Verdict | Evidence |
| --- | --- | --- | --- | --- |
| F02-M01 | 30 s/6-beat and 169.7 s/20-take plans both validate; totals 900 / 5,091 at 30 fps; no legacy 4–7-shot leak | Both validate; beat targets tile [0,900) and [0,5091) exactly | awaiting human | `run F02-M01` in `data/factory-qa/smoke` |
| F02-M02 | overlap, unknown provenance, zero denominator → field-specific typed errors | `frame_overlap` on `intervals[i]`, `bad_provenance` on `provenance`, `invalid_frame_rate` on clock | pass | F02-M02 evidence + `TestF02Cases::test_m02_typed_errors` |
| F02-M03 | in-place edit of accepted plan fails; child revision records parent hash + reason; dependents invalidated | `accept()` on accepted raises `not_draft`; `revise()` yields rev 2 with `parent_hash` + `revision_reason`; parent → `superseded` | awaiting human (downstream-invalidation verified at F03) | F02-M03 evidence + `TestRevisions` |
| F02-M04 | compatible Jimeng → legacy manifest validates; Vertex/long-haul refuses with reasons | 5-shot Jimeng payload passes `asset_manifest.schema.json`; Vertex→`legacy_conversion_refused` (no faithful source label); 20-take→`shot_count` refusal | pass | F02-M04 evidence + `TestLegacyConversion` |

**Live qualification:** none — no live cases.

**Frozen-contract compatibility:** `schemas/` untouched; conversion validates against them read-only. `test_frozen_contracts_unchanged` passes.

**Timing / external-effect counts:** zero external calls.

**Known limitations / next gate:** schema versions are strings in `contracts/v1`; wire-level JSON Schemas can be generated later from record fields if a non-Python consumer needs them. `ProviderPolicy` and `Authorization` field shapes will be exercised for real when F05 budgets and F15 routing land. Downstream invalidation of prices/reviews is represented (`invalidated_by`, `superseded_by`) and will be enforced transactionally by the F03 store. Next: F03 SQLite store.

## 2026-09-17 repair requalification correction

Historical results above are retained, not treated as current acceptance.
The R01–R43 implementation and regression dispositions are in
[REPAIR-S8-FINDINGS.md](REPAIR-S8-FINDINGS.md), with exact source/evidence in
[REPAIR-S8-EVIDENCE.json](REPAIR-S8-EVIDENCE.json). Real public application
exports, durable process interruption tests and browser/restore checks replace
previously overstated end-to-end claims. This does not sign this module's entire
manual checklist, qualify native paid routes, or change legacy G-gates.
