# F01 — QA harness, deterministic fixtures and fake providers

**Module / owner / source revision / completion date:** F01 · Devin · `feat/factory` after F00 checkpoint · 2026-09-16

**Scope implemented:** workspace lifecycle (`init` refuses occupied roots), 144-case registry with module prerequisites and levels, finite fault registry, persistent fake provider with durable effects independent of any app DB, deterministic clock/IDs, network guard (socket + DNS), fixture manifest + materializers, `cases/run/inspect/evidence` CLI, sanitized evidence records with required reviewer path. Deferred: real child-process kill/restart drills land in F07/F34 when a worker process exists; Hypit renders are not part of F01.

**Public interfaces and files changed:**
- `modules/factory/qa/` — `__main__.py` (CLI), `workspace.py`, `registry.py`, `faults.py`, `evidence.py`, `views.py`, `cases_f01.py`
- `modules/factory/testing/` — `clock.py`, `ids.py`, `fakes.py`, `netguard.py`, `fixtures.py`
- `tests/factory_fixtures/` — `manifest.json` + 14 fixture dirs with tracked JSON data
- `tests/test_factory_qa.py` — 25 tests

**Prerequisite versions and migration impact:** none; additive package, no schema changes.

**Targeted automated commands and results:** `pytest tests/test_factory_qa.py` → 25 passed.

**Full offline suite:** `pytest tests -q` → **369 passed in 39.8s** (344 baseline + 25 new), zero skips/failures.

**Manual cases (offline; agent-executed, human sign-off pending):**

| Case | Expected | Actual | Verdict | Evidence |
| --- | --- | --- | --- | --- |
| F01-M01 | Deterministic data, stable identity, no duplicate effects on repeat | `run F01-M01 --run-id r1` twice: second invocation returns the recorded result without re-executing; registry reports exactly 144 cases; fake submit/poll/download counted once | pass (agent) | `data/factory-qa/pytest` runs + case record |
| F01-M02 | Absent module, unknown fault, occupied init → explicit errors | `F05-M01` reports `missing_prerequisite`; `faults.validate(["rm-rf-everything"])` raises KeyError; `init` on occupied root refuses, files survive | pass (agent) | `tests/test_factory_qa.py::test_cli_run_unknown_and_unimplemented` |
| F01-M03 | Fake retains operation; acceptance vs acknowledgement distinguished | `accept-then-timeout` submit raises `response_lost` while remote state holds the accepted op + charge; fresh provider object reconciles by request hash, no resubmission | pass (agent) | `test_accept_then_timeout_keeps_remote_effect`, `test_fake_effects_survive_restart` |
| F01-M04 | Manual verdict required; report includes reviewer + artifact hash | Record stays `awaiting_manual_review` until `record_manual_verdict(reviewer, artifact_revision)`; verdict requires reviewer identity and revision | pass (agent) | `test_manual_verdict_requires_reviewer_and_revision` |

**Live qualification:** not applicable — F01 has no live cases; `--mode live` requires an existing `--authorization-id` and a registered live case.

**Frozen-contract compatibility:** no `schemas/` or `tests/fixtures/` changes; F00 hash manifest unchanged.

**Timing / external-effect counts:** zero real external calls; all effects are fake-provider JSON state under `data/factory-qa/`.

**Rollout / rollback:** harness is additive; delete `modules/factory/` + `tests/factory_fixtures/` + `tests/test_factory_qa.py` to remove. Fake effects live only in QA workspaces.

**Known limitations / next gate:** `inspect` views for F02+ return explicit `unavailable` until their modules land; `connected` mode reserved for registered read-only cases; fixture media bytes vary by ffmpeg build (fixture-lock records generated hashes per workspace, expectations are probe facts). Next: F02 contracts.

## 2026-09-17 repair requalification correction

Historical results above are retained, not treated as current acceptance.
The R01–R43 implementation and regression dispositions are in
[REPAIR-S8-FINDINGS.md](REPAIR-S8-FINDINGS.md), with exact source/evidence in
[REPAIR-S8-EVIDENCE.json](REPAIR-S8-EVIDENCE.json). Real public application
exports, durable process interruption tests and browser/restore checks replace
previously overstated end-to-end claims. This does not sign this module's entire
manual checklist, qualify native paid routes, or change legacy G-gates.
