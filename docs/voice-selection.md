# Voice audition — pick ONE channel voice (M7)

## API setup — 2026-09-15

The user-provided ElevenLabs credential is stored in the gitignored
`config/secrets.toml` with owner-only permissions. Read-only account, voice list
and model checks succeeded. The active Starter account returned 30 voices and
lists the configured `eleven_v3` TTS model. No audio was generated by these checks;
generation permissions and the actual narration path still require a live test.

The channel voice is still unset. Verification evidence, excluding credentials
and personal account details, is saved at
`data/production/elevenlabs-setup/verification.json`.

The project now also accepts credentials in its root `.env`; nonempty process
environment values override `.env`, which overrides the private TOML file.
Empty placeholders keep the existing credential. See
[audio provider setup](audio-providers.md) for Google TTS/music options and the
remaining access checks. The production voice provider is still ElevenLabs.

## Audition

Audition 3 ElevenLabs voices on the same 20s script, blind-listen, pick one.
Pin the winner in `config/system.toml` → `[voice].voice_id`.

| Candidate | voice_id | Notes |
|---|---|---|
| A — warm narrator (e.g. "Adam"-class deep male) | | |
| B — bright explainer (fast, high clarity) | | |
| C — calm storyteller (mystery/history lean) | | |

Script for audition: use `tests/fixtures/contracts/shot_list.sample.json`
`voice_text` — it's already TTS-clean.

Checklist:
- [ ] 3 samples generated (mp3, mp3_44100_128)
- [ ] Blind listen vs Edge TTS baseline — ElevenLabs clearly preferred (G7)
- [ ] Winner pinned in system.toml; characters used logged to cost ledger
- [ ] `voice_id` set → `python -m modules.voice` unblocked
