# Safe (non-secret) factory configuration — copy to config/factory.toml.
# Secrets stay in config/secrets.toml (gitignored) or the environment;
# they are never read from this file and never reach frontend builds.

api_port = 8100
worker_port = 8101
data_root = "data/factory"
drive_folder_id = "1XQU20m_xk5030kAxbbIumeHYkrRPkPbs"
log_level = "info"
media_max_mb = 200
