# Copy this file to secrets.toml and fill in real values.
# config/secrets.toml is gitignored. NEVER commit real keys.

YOUTUBE_API_KEY = ""           # YouTube Data API v3 key — M1 radar, M10 readback
PEXELS_API_KEY = ""            # free key — M6 stock fallback
ELEVENLABS_API_KEY = ""        # M7 voice

LLM_PROVIDER = "kimi"          # kimi | deepseek | openai-compatible
LLM_API_KEY = ""
LLM_BASE_URL = ""              # only for openai-compatible providers
LLM_MODEL = ""                 # e.g. kimi-k3, deepseek-chat

# M10 own-channel analytics (YouTube Analytics API, OAuth)
YT_ANALYTICS_CLIENT_SECRET = ""  # path to OAuth client secret JSON
YT_ANALYTICS_TOKEN = ""          # path to stored OAuth token JSON
